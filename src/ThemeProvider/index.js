// Adapter for emotion-theming, that exports the ThemeProvider as a default
import * as theming from 'emotion-theming'

export const { withTheme, ThemeProvider } = theming

export default ThemeProvider
